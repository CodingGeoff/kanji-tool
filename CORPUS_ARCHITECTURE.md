# 大规模合法语料与多数据库联合检索架构

## 法律边界

**不会抓取或保存 NHK / NHK NEWS WEB EASY 全文。** NHK 官方说明，News Web Easy 的画面共享、音声播放和文言朗读可能需要许可，个人不会获得需要许可的使用授权；分享文章 URL 通常不需要许可。NHK 网络服务条款还禁止超出著作权例外的复制、改写和二次利用。因此 NHK 在来源注册表中固定为 `link_only`，正文导入会被程序拒绝。

可合法批量接入的第一梯队：

- Tatoeba 官方导出：CC BY 2.0 FR，另有 CC0 子集；
- 日语 Wikipedia 官方 dump：CC BY-SA/GFDL，必须署名、注明改动并遵守相同方式共享；
- 日语 Wikinews 官方 dump：按项目当前许可保存（历史上为 CC BY 2.5，导入时再次核验）；
- e-Gov 法令 API：法令正文及政府开放数据，保留“出典：e-Gov法令検索（デジタル庁）”；
- 国会会议录 API、KAKEN 研究摘要等明确 CC BY 的政府/学术开放数据；
- SNOW T15 易懂日语平行语料（CC BY 4.0）、JSUT 的许可明确文本子集；
- ONCOJ 上代日语语料（CC BY 4.0），只作为古典/语言史专门域，不混入现代日语难度；
- 青空文库：只导入逐作品确认已进入公版的正文，不能因网站名称而假定所有文件均可任意使用。

第二梯队仅在逐数据集复核后接入：KFTT、JEC 基础例句、Japanese WordNet、JGLUE 中
许可兼容的子集。Common Crawl/ABEJA-CC-JA/LLM-jp 等规模虽大，但“可用于模型训练”
不自动等于“可把每篇正文重新展示给学习者”；在展示型语言学习应用中默认不导入，除非
逐记录许可、删除请求和再分发条件均得到解决。字幕、商业新闻、问答社区和小说站抓取集同理。

商业教材、新闻站、来源不明真题不抓取。BCCWJ 用于频率/真实性查询，除非持有与分发方式兼容的许可，否则不把正文打包进公开分片。

## 为什么不继续塞进 kanji.db

`kanji.db` 是可变用户状态：课本、SRS、收藏、历史和设置，多设备可能同时修改。大语料则是可再下载、几乎只读的内容。两者放在一起会造成：

- 每次学习进度变化都让几十 GB 二进制文件冲突；
- Git 无法有效增量合并 SQLite；
- Render/GitHub 文件尺寸受限；
- 备份用户数据时被语料拖累；
- 重建索引风险影响个人进度。

因此采用两层：

1. `kanji.db`：小型、可变、继续用现有 dbtool 合并；
2. `corpus_data/shards/corpus-<sha256>.db`：大型、不可变、可丢弃重建。

## 分片规则

- 目标大小：48 MiB；
- 硬上限：80 MiB；
- 每片独立 SQLite，`PRAGMA integrity_check` 后密封；
- 密封后 `VACUUM`，按整个文件 SHA-256 命名并设只读；
- 文本按规范化文本 SHA-256 作为稳定 `uid`；
- 同一文本跨来源重复时，查询按 uid 去重；生产版将另建 provenance 多对多表以保留全部来源；
- `manifest.json` 不是数据真源，可扫描所有分片重建。

内容寻址解决多设备冲突：设备 A 与 B 分别产生新分片，只需合并文件集合；相同分片文件名和内容必然相同，不需要 SQLite 行级三方合并。分片不进 Git，建议对象存储、Release asset、WebDAV、S3/R2 或用户自有同步目录。传输后按 SHA-256 和 SQLite quick_check 双校验。

## 当前实现

`corpus_shards.py`、`federated_search.py` 与现有 `rag.py` 已实现：

- 明确许可的来源白名单；
- NHK 正文硬拒绝；
- staging 创建与幂等导入；
- 48 MiB 轮换提示、80 MiB 硬上限；
- 完整性检查、密封、内容寻址重命名；
- manifest 重建；
- `kanji.db` 与跨任意数量分片的联合召回、统一打分、uid 去重和 provenance 合并；
- 分片损坏或缺失时主库降级可用；
- 现有 RAG 搜索自动把分片命中作为 `web` 通道参与排序，无需把全部文档常驻内存；
- 分片文件变化进入查询缓存签名，新增/删除分片后自动失效；
- `GET /api/corpus/federated-search` 独立调试联合检索；
- `GET /api/corpus/shards/status?verify=1` 重建清单并验证文件哈希；
- 每条记录保存 source item ID、URL、作者、许可证、署名、获取时间和内容哈希。

## 下载与更新计划

### Tatoeba

使用官方 bulk exports，不用逐页爬站。下载日语句、translation links 和需要的中文/英文句；校验压缩包，流式解析，批量提交。每周检测官方文件的 ETag/Last-Modified/哈希，只为新增或变化记录生成新分片。CC0 与 CC BY 记录保留各自许可；音频许可逐条不同，默认不下载。

### Wikimedia

使用官方 dump/API，不大规模抓 HTML。月度检测 dump 日期与 SHA 文件：

- Wikipedia：清洗模板/列表/消歧义/参考文献，保留 page ID、revision ID 和页面 URL；
- Wikinews：保留发布日期和文章 ID；
- 遵守署名及 share-alike，导出的衍生文本包附许可证和变更说明。

### 青空文库

读取作品卡版权状态，仅选择公版；保留作品 ID、作者、作品名、底本与校订信息。旧假名转换只能保存为派生字段，原文必须保留，不能以现代化文本覆盖来源正文。

### NHK

仅保存用户主动收藏的标题、URL、日期和学习备注；点击后跳转官方页面。不缓存正文、音声和图片，不绕过登录/robots/技术措施。若未来取得 NHK 书面许可，再作为新的许可策略接入，不能通过配置误开。

## 联合检索路线

当前 `corpus_shards.query` 是可靠的基础跨片查询。大规模生产版分三层：

1. 可重建 catalog：`uid → shard, rowid, source, level, length`；
2. 词法倒排：字符 bigram / FTS 候选召回；
3. 语义向量：分片向量索引召回后，用现有结构相似度和语法信息精排。

查询过程：来源/等级过滤 → 并行候选召回 → uid 去重 → RRF 或加权融合 → 结构/质量/许可证精排。任何索引都可重建，用户状态不与语料索引混存。

## 准确性流水线

下载成功不等于可出题。记录依次经过：

1. 许可和来源字段完整；
2. 文件哈希和格式校验；
3. Unicode、乱码、模板和碎片清理；
4. 文档/段落/句子边界保留；
5. 语言识别、重复与近重复检测；
6. 形态分析、读音交叉检查；
7. 体裁、年代、长度和难度特征；
8. 候选题生成，只能进入 draft；
9. 人工审核和试测后才 approved。

语料“全量”只表示某个官方快照全部下载，不表示所有文本都适合作题。过滤掉版权不清、模板垃圾、断句错误和事实依赖过强的文本，是准确性的必要部分。
