# 题目来源、版权、丰富性与正确性政策

## 1. 先给结论：不存在“自动生成 = 完整正确”

本项目把题目分成三层，且不混淆其证据强度：

1. **自动练习题**：由本地语料派生，数量丰富，适合低风险形成性练习；
2. **人工题库题**：项目原创或获明确授权，逐题写答案、证据与干扰项理由；
3. **表现性任务**：项目原创真实场景，以量规由教师/学习者评价。

只有通过规定审核和试测的人工题库题才可标为 `approved`。自动题即使程序检查全部通过，也不能标成“专家审定真题”。“完整”在这里指蓝图覆盖与证据链完整，不代表世界上所有日语现象均已穷尽。

## 2. 当前仓库的实际题源

### 2.1 自动语法、读音、单句意义题

来自用户本地 `sentences` 表。目前数据库主要包含：

- **Tatoeba** 例句及其可用翻译；
- **日语 Wikipedia** 抽取句；
- **Wikinews**（若用户抓取过）；
- 用户手动添加、课本导入或备份导入的句子；
- 用户导入歌词仅用于原有练习域，受“无译文不制造语义干扰项”规则限制。

每题保存 `source` 和 `sid`，可回溯到本地原句。Tatoeba 句子通常为 CC BY（具体导出和单条许可仍应随数据保存）；Wikipedia 文本通常受 CC BY-SA 及署名/同方式共享条件约束。应用今后导入时必须保存具体来源 URL、作者/贡献者、抓取日期和许可证版本，不能只写 `wikipedia`。

#### 2.1.1 题源范围解析（scope resolution）与多课本稳健性

早期版本的组句练习（`sentence_builder.py`）存在过一个题源泄漏 bug：用户选择"仅本课本"出题时，题目实际上仍会从全库语料库里泄漏出来，且在配置了多本课本时表现不稳定（容易偏向某一本、或把已归档课本也悄悄拉进来）。根因是当时每个出题模块各自手写了一套"book_ids 是否为空 → 该 fallback 成什么 scope"的逻辑，实现之间互相不一致。

修复方式是把 scope 解析收敛成唯一的共享实现——`textbook.resolve_book_scope(scope, book_ids)`（见 `textbook.py`）：

- 未显式指定课本时，`book`/`mixed` scope 自动使用书架里**当前启用（active）**的全部课本，已归档/停用课本不会被静默拉入，多课本场景下不再偏向某一本；
- 传入的 `book_ids` 会先按书架里真实存在的课本 id 做校验过滤，无效 id 被丢弃而不是报错中断；
- `scope='book'` 但书架为空（没有任何课本）时，明确降级为 `corpus` 并在返回值里带上 `note='no_books_fallback_corpus'`，调用方（前端）必须把这个信号展示给用户（"书架为空，已自动改用全库语料"），而不是悄悄换源却不提示；
- 返回值里的 `auto_all` 标记"是否因未显式选课本而自动用了全部课本"，同样应该在前端提示，避免用户以为自己选的是某一本、实际却在用全部。

**约定**：任何新出题模块（组句 `sentence_builder.py`、挖空 `textbook.py` 的 `make_cloze_multi`、听力 `listening.py`、AI 起草 `ai_item_writer.py`）在处理"课本 vs 语料库"的题源范围时，一律调用 `textbook.resolve_book_scope()`，不得自行重新实现这段逻辑。这是防止同类"课本范围泄漏/多课本不稳健"问题在新模块里重演的唯一保障——三个自动出题模块目前都已接入并有专门回归测试覆盖（`test_scope_resolve.py` 直接测试该函数本身；`test_builder_scope.py`/`test_listening.py` 分别测试组句与听力两个调用方）。

### 2.2 多题共用篇章

`assessment_bank.py` 当前的 N3 通知、N2 意见文和 N1 双文本由本项目为测评目的原创，不是 JLPT 真题，也没有复制商业教材。元数据明确写为：

```json
{"kind":"project_original","author":"kanji-tool assessment team","adapted":false}
```

它们默认是 `draft`，只能试测；程序不会在默认 `approved` 组卷中静默使用。教师须完成六项检查后才能批准。

### 2.3 写作与口语

`performance_tasks.py` 的邮件、提案、资料综合、服务协商、礼貌拒绝与学术答辩均为项目原创任务。它们参考 Can-do/任务型评价原则，不复制官方试题。

### 2.4 听力（自动出题）

`listening.py` 只用规则引擎从本地已有语料/课本句子出题，不引入新文本、不调用任何外部模型：

- **meaning（听后选义）**：句子本身已有译文才出题（无译文=无法核验语义，直接跳过，不臆造译文）；干扰项来自同批次真实存在的其他译文，并按语言过滤，避免中/英译文混进同一组选项；
- **discriminate（听音辨句）**：不需要译文；换词候选只从同批次真实出现过的名词里挑，绝不凭空生成假词，前端会显示"干扰项换成了语料里真实出现过的『XX』"以标注可核验性；
- 两种题型都不需要额外人工审核队列，因为干扰项和答案都可从本地语料机械核验，不涉及语义判断的主观正确性。

这两种题型是规则引擎能自动、安全出的听力题的**上限**——更进一步的"主旨/推断/结合上下文理解"类听力理解题，机械规则无法生成，见 2.5。

### 2.5 AI 辅助起草题（`ai_item_writer.py`）：draft → 人工复核 → approved

真正的听力/阅读理解题（需要"读懂"文本、判断主旨/推断/因果）超出规则引擎能力范围，本项目用用户自备 Key 的 LLM（复用 `ai_translate.py` 已有的 Provider/Key 配置，见 `/api/translate/config`，不单独收集或托管密钥）辅助起草，但严格遵守"起草 ≠ 可用"：

**起草边界（写在代码里，不是靠自觉）：**

- 只用本地已有、且已在语料许可白名单内的文本作为起草素材（`_sentence_candidates`/`_passage_candidates_from_books`/`_passage_candidates_from_songs`），不会现取网络新文本喂给模型；
- 单句题**必须已有译文**才允许起草（`generate_sentence_item` 对空译文直接拒绝并报错："没有语义锚点，AI 起草的题无法核验对错，拒绝生成"）；
- 每道题的 `rationale`（依据）必须能在原文或译文中找到实际依据（`_grounded()` 校验），通不过校验的候选题直接丢弃，不会用模型的自信语气冒充已核验；
- 篇章题的 `evidence` 字段限定只能引用真实存在的句序标签（`s1..sN`），越界标签会被过滤掉；
- 选项数量、正确答案下标等结构一律做类型/范围校验（`_validate_options`），结构不合法的候选题直接丢弃；
- Provider 级别的失败（未配置 Key、网络失败、鉴权失败、5xx）会立即中止整批起草并如实报错，绝不会"跳过继续"、也不会返回假数据掩盖配置问题。

**状态生命周期（`ai_draft_items` 表，与第 7 节的四态模型一致）：**

```text
draft → reviewed → approved → retired
```

- 起草结果一律落库为 `draft`，`source.kind='ai_generated'`，并记录 `provider`/`model`/`based_on_sid`/`based_on_source`/`generated_at` 完整可回溯来源；
- `draft`/`reviewed` 状态下的题**不会**出现在任何正式练习/组卷路径里——`/api/ai-items/approved` 是听力/阅读练习唯一的消费入口，它硬编码只返回 `status='approved'` 的行；
- 批准前必须完成全部 4 项复核清单（`content_correct` 内容正确 / `grounded_in_source` 确有原文依据 / `level_fit` 难度匹配 / `language_natural` 表达自然），任一项未勾选，后端直接拒绝把状态改成 `approved`（`review_draft()` 强制校验，不是前端自觉遵守的软约束）；
- 复核人署名（`reviewer`）随批准记录一起持久化，留痕可追溯是谁批准的。

这套机制的核心立场：**AI 能帮忙起草候选题，但不能替代人工判断题目是否正确、是否有依据、难度是否匹配**——这正是第 5 节"机器门禁只能排除低级错误"原则在 AI 辅助场景下的延伸，而不是绕过它。

## 3. 计划采用与不采用的外部来源

| 来源 | 用途 | 可否直接入库 | 注意事项 |
|---|---|---:|---|
| Tatoeba/Tanaka Corpus | 例句、搭配、读音语境 | 可以，须署名 | 通常 CC BY；社区句质量不等于专家审定 |
| Wikipedia/Wikinews | 百科、新闻体裁候选文本 | 有条件 | 保存页面版本、URL、许可证与改写记录；不把原文直接包装成原创 |
| 青空文庫 | 文学、旧文体 | 公版作品可 | 逐作品确认版权；旧语法不代表现代通用日语 |
| いろどり/JF 教材 | Can-do 与生活场景参考 | 按具体利用指针 | “免费”不等于任意复制；优先参考目标后原创题文 |
| BCCWJ | 频率、体裁与真实性核验 | 不直接复制正文 | 正文受授权条件约束；用作查询和校准，不随仓库分发原文 |
| JLPT 官方样题 | 题型规格与认知操作参考 | 不批量复制 | 不冒充官方、不泄露真题；原创平行题 |
| 商业教材/N级题库 | 专家对照 | 不复制 | 无明确授权不得抓取、改写或分发 |
| 用户课本/歌词 | 个人学习 | 仅用户授权范围 | 不进入公共题库；保留本地，不宣称可再分发 |

许可不是靠来源名称推断。每条公共题材至少保存 `kind/title/author/url/license/retrieved_at/adapted`；缺任何关键字段即不能发布。

## 4. 如何保证“丰富”

### 4.1 不是靠随机数量，而是靠蓝图配额

每套正式题先定义覆盖矩阵，再抽题：

- 能力：文字词汇、语法、局部理解、信息提取、整合、推论、主旨、评价、语用；
- 技能：读、听、写、说、互动；
- 文本：通知、邮件、广告、对话、叙事、说明、新闻、评论、学术短文、图表/非连续文本；
- 场景：生活、学校、职场、公共服务、媒体、学术；
- 语体：口语/书面、普通/礼貌、正式/非正式；
- 长度：短文、中文、长文、多文本；
- 认知过程：定位、改写、整合、推论、评估、产出。

每个格子有最低题量和最大曝光率。不能用 100 道助词题冒充“题库很大”。系统返回 `shortfalls`，缺题就承认缺题。

### 4.2 来源多样性限制

同一试卷设置：

- 同一原文最多一个题组；
- 同一作者/页面/歌曲/课本章节的占比上限；
- 同一语法点、同一答案表面形的重复上限；
- 生活/职场/学术体裁按目标等级配额；
- N1/N2 必须出现整体主张或多文本整合，不能全是单句。

### 4.3 建议规模

进入稳定运营前，每级至少建设：

- 文字词汇 150–250 道已试测题；
- 语法/文章语法 120–200 道；
- 20–30 篇短文题组、15–20 篇中长文题组；
- N1/N2 各 15 组双文本；
- 15–20 份信息检索材料；
- 30–50 个听力任务脚本；
- 写作与口语各 12–20 个等价任务。

这些是建设目标，不是当前已达到的数量。

## 5. 如何保证“正确”

### 5.1 机器门禁只能排除低级错误

自动检查：

- 题干完整、无乱码；
- 四个选项唯一且答案存在；
- 原文可回溯；
- 答案证据和解释非空；
- 接续和形态规则可行；
- 无译文时不自动制造语义干扰项；
- 选项长度、格式和语法形态不过度泄露答案；
- 固定 seed 可重现。

机器检查不能证明作者立场推断唯一、语用自然或等级恰当。

### 5.2 六项人工审核

`assessment_bank.review` 在批准前强制：

1. `answer_unique`：答案唯一，其他选项为何错可解释；
2. `evidence_exact`：证据句准确支持答案；
3. `level_fit`：文本、任务和选项符合目标等级；
4. `bias_checked`：不依赖无关文化常识，不含不必要偏见；
5. `language_natural`：由高水平日语使用者检查自然度、语体和事实；
6. `source_checked`：来源、许可、改写和署名完整。

理想流程是作题人与复核人不同；高风险考试至少双人独立作答并达成一致。

### 5.3 认知访谈和预测

审核后仍只是“内容上可用”。正式化前应：

- 让目标等级学习者口述作答过程，确认使用了目标能力而非猜题技巧；
- 记录开放作答中的常见错误，形成实证干扰项；
- 检查正确率、区分度、各干扰项选择率、耗时和信心；
- 复核过易、过难、负区分、无人选择干扰项；
- 样本充足后才拟合 IRT，并通过锚题做跨卷等值。

当前 `/api/exam/item-analysis` 已采集第一阶段所需数据，但个人本地样本不能支持稳定 IRT。

## 6. 如何保证“完整”

完整性由四份可审计产物定义：

1. **构念表**：每个能力的定义与不能推断的边界；
2. **蓝图**：每级、每技能、每体裁和认知过程的题量配额；
3. **题目档案**：来源、许可、答案、证据、干扰项理由、审核记录、统计；
4. **效度档案**：专家意见、认知访谈、项目统计、版本变更和使用后果。

每次组卷输出 planned vs actual coverage；任何关键格子缺题则标记不完整，不用其他题型填充掩盖。

## 7. 状态生命周期

```text
draft → reviewed → approved → retired
```

- `draft`：作者初稿/AI辅助初稿，仅内部查看；
- `reviewed`：完成一轮语言与内容复核，可小规模预测；
- `approved`：完成全部清单并满足最低试测条件，可正式组卷；
- `retired`：曝光过高、内容过时、统计异常或版权变化，停止使用。

当前原创篇章全部有意保持 `draft`。这比把尚未由教师审核的内容宣称为“准确完整题库”更负责任。
