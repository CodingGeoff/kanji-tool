# 日语汉字注音引擎技术文档

> 深入解析本项目 `furigana.py` 如何实现「汉字 → 平假名读音」的高准确率自动标注。
> 面向对算法实现不太熟悉、但想了解原理的读者（例如需要向日语教师解释技术方案时）。
> 文中每一条输入输出、每一个 MeCab 分词结果，都是从本引擎真实运行结果里直接取出来的。

---

## 目录

1. [为什么汉字注音这么难](#1-为什么汉字注音这么难)
2. [整体架构](#2-整体架构)
3. [深度剖析：多音字「行」是怎么分对的](#3-深度剖析多音字行是怎么分对的)
4. [第 2 层：数词与助数词规则引擎](#4-第-2-层数词与助数词规则引擎)
5. [第 3 层：N-best 歧义检测](#5-第-3-层n-best-歧义检测)
6. [第 4 层：双引擎交叉验证](#6-第-4-层双引擎交叉验证)
7. [第 5 层：未知词零猜测](#7-第-5-层未知词零猜测)
8. [送假名对齐](#8-送假名对齐)
9. [深度剖析：字读音提取（字素对齐）](#9-深度剖析字读音提取字素对齐)
10. [关键辅助算法](#10-关键辅助算法)
11. [数据流与数据结构](#11-数据流与数据结构)
12. [完整例子汇总](#12-完整例子汇总)
13. [局限与改进方向](#13-局限与改进方向)

---

## 1. 为什么汉字注音这么难

给日语汉字自动标注读音（振假名/ルビ），本质是把一个「表意文字序列」映射成「假名序列」。难点在于**同一个汉字，读音随上下文变化**，大致有六类：

### 1.1 多音字（音读 vs 训读）

同一个「生」，在不同词里读音完全不同：

| 词 | 「生」的读音 | 类别 |
|---|---|---|
| 生(い)きる | い | 训读 |
| 学生(せい) | せい | 音读 |
| 一生(しょう) | しょう | 音读（汉音） |

### 1.2 同形异音词

表面写得一样，读法取决于语境：

| 句子 | 「行」的读音 | 归属动词 |
|---|---|---|
| 学校に行った | い | 行く |
| 会議を行った | おこな | 行う |

### 1.3 熟字训

整词有特殊读音，拆成单个字就错：

| 词 | 读音 | 拆开会错成 |
|---|---|---|
| 今日 | きょう | こん+にち |
| 昨日 | きのう | さく+じつ |
| 風邪 | かぜ | ふう+じゃ |
| 大人 | おとな | だい+じん |

### 1.4 连浊（音便）

清音在复合词中浊化：

| 词 | 读音 | 连浊点 |
|---|---|---|
| 3階 | さん**が**い | かい → がい |
| 火曜日 | かよう**び** | ひ → び |
| 3本 | さん**ぼ**ん | ほん → ぼん |

### 1.5 数词 + 助数词（量词）

数字和量词搭配，读法极其不规则：

| 例子 | 读音 | 不规则点 |
|---|---|---|
| 1分 | いっ**ぷ**ん | いち→いっ、ふ→ぷ |
| 3本 | さん**ぼ**ん | ほん→ぼん |
| 20歳 | **はたち** | 完全换词 |
| 1日 | **ついたち** | 完全换词 |
| 4月 | **し**がつ | よん→し |

### 1.6 送假名

汉字只占整词读音的一部分，假名部分要剥掉：

| 词 | 整词读音 | 汉字部分读音 |
|---|---|---|
| 食べる | たべる | 食 → た |
| 美味しい | おいしい | 美味 → おい |

如果只靠「查字典」给每个汉字一个固定读音，以上六类几乎全错。所以本引擎采用**「词典引擎 + 规则引擎 + 交叉验证 + 字素对齐」的分层防错架构**。

---

## 2. 整体架构

```
                    输入：一段日文文本
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第1层  MeCab(fugashi) + UniDic 词典            │
   │        · 把句子切成「词」(形态素)               │
   │        · Viterbi 算法在词格上找最优切分+读音     │
   │        · 每个词自带词典读音(kana)              │
   └───────────────────────┬───────────────────────┘
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第2层  数词 + 助数词 规则引擎                   │
   │        · 修正 UniDic 对「10分/3本/1日」的错误   │
   └───────────────────────┬───────────────────────┘
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第3层  N-best 歧义检测                          │
   │        · 同形异音词(行った/辛い) → 标警示+候选   │
   └───────────────────────┬───────────────────────┘
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第4层  Sudachi 第二引擎交叉验证                  │
   │        · 双引擎一致=高置信；分歧=降置信并给读音   │
   └───────────────────────┬───────────────────────┘
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第5层  未知词零猜测原则                          │
   │        · 词典无据 → 明确标「读音未知」不瞎猜     │
   └───────────────────────┬───────────────────────┘
                           │
                    输出：token 列表（词 + 整词读音）
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 扩展层  字读音提取（字素对齐 Grapheme-to-Phoneme）│
   │        · 把「整词读音」拆到「每个汉字」           │
   │        · 用于课本「单字」的假名标注               │
   │        · 合計→合(ごう)+計(けい) 这类拆解          │
   └───────────────────────┬───────────────────────┘
                           │
                    输出：每个汉字各自的读音
```

**两层分工**：

- 前五层回答「**这个词怎么读**」——输出整词读音。
- 扩展层回答「**这个词里每个字分别怎么读**」——把整词读音精确拆到字。

---

## 3. 深度剖析：多音字「行」是怎么分对的

这是最能体现「上下文决定读音」的例子。核心结论先说：**「行った」能标对，不是靠给「行」一个固定读音，而是靠形态素解析（分词）**——UniDic 词典里「行く」「行う」是两个完全不同的词条，句子切分后归到哪个词条，读音就跟着定。

### 3.1 第一步：MeCab 把「行った」切成了什么

代码入口在 `furigana.py`，先加载词典、再切分：

```21:21:furigana.py
_tagger = fugashi.Tagger()
```

```460:461:furigana.py
def annotate(text: str):
    words = list(_tagger(text))
```

直接看 MeCab 的原始输出（未经本项目任何后处理），两个句子的差别一目了然：

```
学校に行った。
  行っ  kana='イッ'     cType='五段-カ行'   cForm='連用形-促音便'  orthBase='行く'   kanaBase='イク'
  た    kana='タ'      pos1=助動詞

会議を行った。
  行っ  kana='オコナッ' cType='五段-ワア行' cForm='連用形-促音便'  orthBase='行う'   kanaBase='オコナウ'
  た    kana='タ'      pos1=助動詞
```

这里有两个关键点：

**① 「行った」不是当成一个词，而是切成两个形态素**：

```
行った = 行っ（动词活用形） + た（过去助动词）
```

UniDic 的切分规则把动词连用形促音便「行っ」和助动词「た」拆开。真正要区分读音的是「行っ」这个片段。

**② 「行っ」对应两个不同的词典词条，靠三个字段区分**：

| 字段 | 含义 | 学校に行った | 会議を行った |
|---|---|---|---|
| `cType` | 活用型 | 五段-**カ行** | 五段-**ワア行** |
| `orthBase` | 词典原形 | **行く** | **行う** |
| `kana` | 读音 | **イッ**（いっ） | **オコナッ**（おこなっ） |

「行く」是カ行动词（い-く），「行う」是ワア行动词（おこな-う），它们活用型、读音、原形都不同。**读音不是查「行」这个字，而是查「行っ」这个活用形词条。**

### 3.2 第二步：Viterbi 怎么二选一（消歧核心）

MeCab 先给句子画「词格」，每个位置都可能有多个候选，然后 **Viterbi（动态规划）找总代价最小的一条路**。看两个句子的 N-best 前 5 条路径：

```
学校に行った。  （最优路径里「行っ」读 いっ）
  路径1 ★ 行っ=イッ    (行く)
  路径2   行っ=オコナッ (行う)   ← 也有这个候选，但代价更高

会議を行った。  （最优路径里「行っ」读 おこなっ）
  路径1 ★ 行っ=オコナッ (行う)
  路径2   行っ=イッ    (行く)   ← 也有这个候选，但代价更高
```

**关键**：两个句子里「行っ」都同时有「行く」和「行う」两种读法候选，但 Viterbi 用「连接代价」区分——词典里记录了「名詞＋に＋行く」比「名詞＋に＋行う」更常见（去学校用「行く」），而「会議を＋行う」比「会議を＋行く」更常见（举行会议用「行う」）。所以：

- 「学校**に**行った」→ 「行く」代价更低 → 选 `イッ`
- 「会議**を**行った」→ 「行う」代价更低 → 选 `オコナッ`

> **通俗版**：Viterbi = 「把每个岔路口的所有选择都记下来，最后回退找出全局最优的一条路」，而不是贪心地每一步选当前最好。

### 3.3 第三步：本项目在 MeCab 输出之上还做了什么

拿到 `kana='イッ'` 后，`annotate` 还有几步加工。

**① 片假名 → 平假名**

```578:578:furigana.py
        reading = kata_to_hira(kana)
```

`イッ` → `いっ`，`オコナッ` → `おこなっ`。

**② 提取词典原形（供词组展示，避免活用残段）**

```607:610:furigana.py
        dw = w.feature.orthBase if (w.feature.orthBase and w.feature.orthBase != '*') else surface
        dk = w.feature.kanaBase if (w.feature.kanaBase and w.feature.kanaBase != '*') else kana
        dr = kata_to_hira(dk)
```

把活用形「行っ」归到原形「行く / 行う」，读音归到「いく / おこなう」。这就是 `dw`/`dr` 字段——点击「行」时展示的是「行く」，而不是残缺的「行っ」。

**③ 送假名对齐：把「いっ」拆到字**

```633:640:furigana.py
        aligned = _align(surface, reading)
        if aligned is None:
            tokens.append({'s': surface, 'r': reading, ...})
        else:
            for seg, ruby in aligned:
                tokens.append({'s': seg, 'r': ruby, 'w': surface, 'wr': reading, ...})
```

「行っ」的表面形是「行（汉字）+ っ（促音）」，`_align` 把读音「いっ」拆成「い」+「っ」：

```
行っ → 行(い) + っ(无需注音)
```

**④ 置信度 + 候选读音（第 3 层歧义检测）**

```612:631:furigana.py
        conf, alts = ('high' if i in boosts else 'mid'), set()
        nb = nbest.get(span)
        if nb and len(nb) > 1:
            alts |= {a for a in nb
                     if a != reading and not is_equiv_reading(a, reading)
                     and not is_fragment_alt(a, reading)}
        ...
        if alts:
            conf = 'low'
```

`_nbest_alternatives`（第 333 行）用 N-best 找到「行っ」的另一候选读音「おこなっ」，最终 token 带上：

```python
{'s': '行', 'r': 'い', 'w': '行っ', 'wr': 'いっ',
 'dw': '行く', 'dr': 'いく', 'c': 'low', 'alt': ['おこなっ']}
```

`c='low'` + `alt=['おこなっ']` 告诉前端：**这个字在这里有歧义，另一个读法是「おこなっ」**。

### 3.4 完整链路回顾

```
输入：学校に行った。
  │
  ▼ ① MeCab + UniDic 分词（Viterbi 选最优路径）
  「行っ」→ kana=イッ, orthBase=行く, cType=五段-カ行
  │
  ▼ ② 片假名转平假名：イッ → いっ
  ▼ ③ 原形提取：行っ → 行く / いっ → いく
  ▼ ④ 送假名对齐：行っ → 行(い) + っ
  ▼ ⑤ N-best 歧义检测：alt=['おこなっ'], c='low'
  │
  输出 token：{'s':'行', 'r':'い', 'dw':'行く', 'alt':['おこなっ']}
```

「会議を行った」走完全相同流程，只是第 ① 步 Viterbi 选的是 `オコナッ / 行う / 五段-ワア行`。

---

## 4. 第 2 层：数词与助数词规则引擎

UniDic 对普通词很准，但对「数字 + 量词」经常出错。下面是**真实 MeCab 输出 vs 引擎修正结果**的对照——这就是第 2 层存在的意义。

### 4.1 「10分」：MeCab 把「分」读成「ぶん」

MeCab 原始输出（注意数字「10」本身没有读音，kana 为空，被略去）：

```
'分'  kana='ブン'   ← MeCab 读「ぶん」
```

正确应是「じゅっ**ぷん**」。规则引擎查表修正：

```python
'分': {'base':'ふん', 1:'いっぷん', 3:'さんぷん', 6:'ろっぷん', 8:'はっぷん', 10:'じゅっぷん'}
```

最终结果：

```
10 → じゅっ     分 → ぷん
```

### 4.2 「3本」：MeCab 把「本」读成「ぽん」

MeCab 原始输出：

```
'本'  kana='ポン'   kanaBase='ホン'   ← MeCab 读「ぽん」，但正确是「ぼん」
```

规则引擎查表：

```python
'本': {'base':'ほん', 3:'さんぼん'}
```

最终结果：

```
3 → さん      本 → ぼん     （连浊：ほん → ぼん）
```

### 4.3 「二十歳」：MeCab 完全读错

MeCab 原始输出：

```
'二十' kana='ニジュウ'     '歳' kana='サイ'   ← MeCab 读「にじゅうさい」
```

正确是「**はたち**」。规则引擎有专门的融合读法表：

```python
'歳': {'base':'さい', 20:'はたち'}
```

因为「二十歳」无法拆成「二十 + さい」，所以标记为 FUSED，整体标注：

```
二十歳 → はたち（整体，不拆）
```

### 4.4 「4月1日」：日期读法

MeCab 原始输出：

```
'月' kana='ガツ'     '日' kana='カ'   ← MeCab 读「よんがつ」「いちか」，全错
```

正确是「しがつ ついたち」。规则引擎修正：

```
4 → し      月 → がつ     1日 → ついたち（整体）
```

### 4.5 数字 + 单位混合（6千、1万6千）

MeCab 把「六千」读成整词「ロクセン」，把「1万6千」里的数字「1」「6」丢掉：

```
六千       MeCab: '六千' kana='ロクセン'   （整词，没拆到字）
1万6千     MeCab: '万' kana='マン' + '千' kana='セン'  （数字「1」「6」丢失）
```

引擎的数字规则 + 单位读音表逐段拆分：

```
六千     → 六(ろく) + 千(せん)
1万6千   → 万(まん) + 千(せん)
```

> 这里的「数字+单位」混合是本引擎专门处理的难点——数字部分走规则引擎（`num_to_kana`）、单位汉字（千/万/億）走固定读音表，再把数字读音从整段读音里剥掉。

---

## 5. 第 3 层：N-best 歧义检测

有些词即使分对了，仍可能有两个读音（真歧义）。用 MeCab 的 N-best 看同一片段在不同路径下读音是否分歧。

真实运行输出：

```
输入：今日は昨日より暑い。
  {'s': '今日', 'r': 'きょう', 'c': 'low', 'alt': ['こんにち']}

输入：風邪を引いた。
  {'s': '風邪', 'r': 'かぜ', 'c': 'low', 'alt': ['ふうじゃ']}
```

「今日」主读音取「きょう」，同时把候选「こんにち」放进 `alt` 并标 `low`。

> **不是所有读音差异都是真歧义**。「火曜日」的「かようび / かようひ」只是清浊差，会被静默归一，不标警示——否则警示噪音会淹没真正值得注意的多音字。见第 10 节「清浊归一」。

---

## 6. 第 4 层：双引擎交叉验证

引入第二个形态素分析引擎 **Sudachi**（模式 C，倾向最长复合词）。两个引擎独立分析，互相印证：

| 情况 | 处理 |
|---|---|
| 两引擎读音一致 | 置信度升为 `high` |
| 两引擎分歧（非清浊差） | 采用主读音，把对方读音列入候选，降置信 |

典型价值：MeCab 的短单位切分可能把「日本人」拼成「にっぽん + じん」（错误），Sudachi 长词模式直接给出「にほんじん」，交叉验证就能纠偏。

> 注：Sudachi 是可选依赖，装了才启用；没装时自动跳过该层。

---

## 7. 第 5 层：未知词零猜测

如果两个引擎都查不到这个词（人名、外来新词等），**绝不乱猜**，而是明确标记 `unk=True`、读音为空。

真实运行输出：

```
输入：ぼくは姜文と王菲のファンです。
  {'s': '菲', 'r': None, 'w': '菲', 'wr': None, 'unk': True}
```

MeCab 对「菲」没有任何读音（kana 为空），引擎就明确标 `unk`，不注音，而不是硬塞一个「ひ」或「フェイ」。

---

## 8. 送假名对齐

词典给出的是「整词读音」，但前端要在「单个汉字」上方标注假名，需要知道整词读音里**哪一段属于汉字、哪一段属于假名**。

以「食べ物」为例，整词读音「たべもの」，表面形由「食（汉字）+ べもの（假名）」组成。做法：

1. 把表面形按「汉字 / 非汉字」分段：`食`（汉字段）+ `べもの`（假名段）
2. 用正则把读音「たべもの」匹配成：汉字段读音 `た` + 假名段 `べもの`
3. 于是「食」→ `た`，完美对齐

真实运行输出：

```
食べ物      → 食(た) + べもの(无注音)
美味しい    → 美味(おい) + しい(无注音)
暑い        → 暑(あつ) + い(无注音)
代わり      → 代(か) + わり(无注音)
```

MeCab 原始输出（动词/形容词活用形会被拆成「词干 + 活用尾」，正是送假名对齐的对象）：

```
風邪を引いた。   '引い' kana='ヒイ'  orthBase='引く'  kanaBase='ヒク'
                 （引い = 引く 的连用形イ音便，送假名对齐后 引→ひ）
```

---

## 9. 深度剖析：字读音提取（字素对齐）

> 这是本引擎最核心、也最体现「准确识别」的部分。

### 9.1 问题定义

词典能给出「合計」整词读「ごうけい」，但学习工具需要在「合」上方标「ごう」、「計」上方标「けい」。**把整词读音切分到每个汉字**，就是「字素对齐（Grapheme-to-Phoneme）」。

难点：日语音读的切分边界无法从读音本身看出来——「ごうけい」可以切成「ごう+けい」，也可以切成「ご+うけい」。所以必须借助**外部知识**。

### 9.2 四层对齐策略（按优先级从高到低）

```
多汉字词 → 策略A 单字/送假名（直接用 token.r）
         → 策略B N-best 逐字切分 + 拼接校验
         → 策略C 音读表 + 动态规划回溯
         → 策略D 训读前缀 + 数字混合
```

#### 策略 A：单字 / 送假名词干 —— 直接用对齐结果

如果 `annotate` 已经把词拆成了「单字」或「汉字词干 + 送假名」，则 token 自带的 `r` 就是该字的读音：

```
多い    → 多(おお)         ← 送假名「い」已剥离
代わり  → 代(か)            ← 送假名「わり」已剥离
館      → 館(かん)          ← 单独成词
```

#### 策略 B：N-best 单字切分 + 拼接校验

对于纯多汉字词（合計、勉強），用 MeCab 的 N-best 强制找「逐字切分」的路径，并**校验所有单字读音拼接后是否等于整词读音**。只有相等才采纳——这是防止把整词读音糊到单字上的关键保险。

```
「合計」的 N-best 路径里包含：
   [合(ゴウ), 計(ケイ)]  → 拼接 = ごうけい = 整词读音 ✓ → 合=ごう, 計=けい

「勉強」：
   [勉(ベン), 強(キョウ)] → べんきょう ✓ → 勉=べん, 強=きょう
```

#### 策略 C：音读表 + 动态规划回溯

N-best 有时拆不出来。典型问题：**某字在「单字」形态下的读音，与它在「复合词」里的读音不同**。

例如「競争」，MeCab 把「競」单字读作「せり / けい」，但「競争」里的「競」读「きょう」——单字候选里根本没有「きょう」。这时启用**内置常用汉字音读表** `_ON_YOMI`（约 300 个高频字），用**动态规划（回溯）**做字素对齐：

```
音读表：
  競 → [きょう, けい]
  争 → [そう]

输入：競争 / きょうそう

DP 过程（dfs(i, pos) = 已切前 i 字、读音消耗到 pos 位置）：
  位置0 试「競」的候选「きょう」(长度3) → 匹配读音「きょう」，剩余「そう」
  位置1 试「争」的候选「そう」  (长度3) → 匹配剩余「そう」，恰好切完 ✓
  结果：競=きょう, 争=そう
```

策略 C 起关键作用的例子：

```
競争 → 競(きょう) 争(そう)     ← 单字候选没有「きょう」，靠音读表
図書 → 図(と) 書(しょ)         ← 单字「図」只有「ず」，音读表补「と」
掲示 → 掲(けい) 示(じ)
閲覧 → 閲(えつ) 覧(らん)
読書 → 読(どく) 書(しょ)
食堂 → 食(しょく) 堂(どう)
講座 → 講(こう) 座(ざ)
企画 → 企(き) 画(かく)
株式 → 株(かぶ) 式(しき)
法則 → 法(ほう) 則(そく)
自由 → 自(じ) 由(ゆう)
```

#### 策略 D：训读前缀匹配 + 数字混合

- **训读词干**：如「気持ち」的词干「気持」读「きも」，其中「持」的训读是「もち」，词干只剩「も」。DP 允许候选读音是原文片段的前缀扩展（「もち」→ 前缀「も」），从而拆出 気=き、持=も。

  ```
  気持ち → 気(き) 持(も)
  頑張る → 頑(がん) 張(ば)
  一人   → 一(ひと) 人(り)
  人通り → 人(ひと) 通(どお)
  彼女   → 彼(かの) 女(じょ)
  ```

- **数字混合**：如「6千」读「ろくせん」，数字部分用规则引擎读出「ろく」，单位「千」用固定读「せん」，剥离后「千」= せん。

  ```
  6千 → 千(せん)
  1万 → 万(まん)
  ```

### 9.3 为什么这套能「准确」

```
核心思想：用「整词读音」作为校验锚点。
```

任何对齐结果，都必须满足「**所有单字读音拼起来 = 整词读音**（清浊归一后）」这一硬约束。这样即便某个字的候选读音有多个，也能被整词读音「逼」出正确的那一个。

### 9.4 完整对齐例子（真实输出）

```
输入：合計は千円です。
  合 → ごう     計 → けい     千 → せん     円 → えん

输入：学部の勉強は楽しい。
  学 → がく     部 → ぶ       勉 → べん     強 → きょう     楽 → たの

输入：図書館へ行った。
  図 → と       書 → しょ     館 → かん     行 → い

输入：特に興味が多い。
  特 → とく     興 → きょう   味 → み       多 → おお

输入：自由な時間を予想する。
  自 → じ       由 → ゆう     時 → じ       間 → かん
  予 → よ       想 → そう

输入：競争の企画と株式の法則。
  競 → きょう   争 → そう     企 → き       画 → かく
  株 → かぶ     式 → しき     法 → ほう     則 → そく

输入：上手に字が上手く書けた。
  上 → じょう   手 → ず       字 → じ       上 → うま     手 → うま     書 → か
```

注意最后一行：同一个「上」，在「上手(じょうず)」里读「じょう」，在「上手い(うまい)」里读「うま」——**同一个字、同一句话里两种读音，都能各自对齐正确**。

背后的 MeCab 分词数据：

```
上手    kana='ジョウズ'   orthBase='上手'    （名詞，读 じょうず）
上手く  kana='ウマク'    cType='形容詞'  orthBase='上手い'  （形容词连用形，读 うま）
```

两个「上手」在词典里是**不同的词条**（名詞 vs 形容詞），读音自然不同，与「行った」是同一套机制。

---

## 10. 关键辅助算法

### 10.1 清浊归一（连浊忽略）

日语复合词中清音常浊化（连浊），但「か vs が」在判断读音是否正确时应该视为「同一个音」：

```python
_DEVOICE = {'が':'か', 'ざ':'さ', 'だ':'た', 'ば':'は', 'ぱ':'は', ...}
_devoice('さんがい')   # → さんかい
```

真实例子：

```
3階 → 階(がい)     ← 字读音保留连浊「がい」，但校验时按清音「かい」匹配
```

### 10.2 语体等价变体

口语与书面语的等价读法，不算歧义：

```
わたくし = わたし、いう = ゆう、にほん = にっぽん、よい = いい
```

### 10.3 促音 / 长音变体

音读表里对促音（小「っ」）做了变体收录，因为同一个字在复合词里会促音化：

```
失 → [しつ, しっ]     失敗(しっぱい) 里读「しっ」
接 → [せつ, せっ]     接触(せっしょく) 里读「せっ」
発 → [はつ, はっ, ほっ]
各 → [かく, かっ]     各国(かっこく) 里读「かっ」
```

### 10.4 缓存（LRU）

字素对齐 `_split_kanji_reading` 和单字候选 `_char_candidates` 都加了 `functools.lru_cache`。同一批课本里「合計」可能出现几十次，第一次算好后直接命中缓存，避免重复做昂贵的 N-best 查询。

---

## 11. 数据流与数据结构

### 11.1 token 结构（第 1-5 层输出）

```python
{
    's':   '合計',          # 表面形（原文片段）
    'r':   'ごうけい',      # 该片段的平假名注音（无汉字则为 None）
    'w':   '合計',          # 所属整词
    'wr':  'ごうけい',      # 整词读音
    'dw':  '合計',          # 词典原形（用于活用词，如「行っ→行く」）
    'dr':  'ごうけい',      # 原形读音
    'c':   'high',          # 置信度 high/mid/low
    'alt': ['...'],         # 候选读音（仅歧义时）
    'unk': False,           # 是否词典未收录
}
```

### 11.2 字读音提取（扩展层输出）

```python
extract_char_readings(tokens) → [('合', 'ごう'), ('計', 'けい'), ...]
```

课本「字」索引把每个汉字对应到这个字读音，存入 `book_kanji.reading`，前端用 `<ruby>計<rt>けい</rt></ruby>` 渲染。

### 11.3 两个提取函数的区别

| 函数 | 输出 | 用途 |
|---|---|---|
| `extract_kanji_words` | (汉字, 所属词, **词读音**) | 全库「字→词」索引，点字看例句/词 |
| `extract_char_readings` | (汉字, **字读音**) | 课本「单字」假名标注，字上方的ルビ |

两者语义不同：一个是「这个字属于哪个词、那个词怎么读」，一个是「这个字本身怎么读」。

---

## 12. 完整例子汇总

### 12.1 难点分类对照（含 MeCab 原始 vs 引擎修正）

| 输入 | 难点 | MeCab 原始 | 引擎结果 |
|---|---|---|---|
| 学校に行った | 多音字「行」 | 行っ=イッ（カ行） | 行→い（行く） |
| 会議を行った | 多音字「行」 | 行っ=オコナッ（ワア行） | 行→おこな（行う） |
| 上手に字が上手く書けた | 多音字「上」 | 上手=ジョウズ / 上手く=ウマク | 上→じょう／上→うま |
| 生き物の生態を生涯研究 | 多音字「生」 | 生き物=イキモノ / 生態=セイタイ / 生涯=ショウガイ | 生→い／せい／しょうがい |
| 今日は良い天気 | 熟字训 | 今日=キョウ | 今日→きょう |
| 昨日より暑い | 熟字训 | 昨日=キノウ | 昨日→きのう |
| 風邪を引いた | 熟字训 | 風邪=カゼ | 風邪→かぜ |
| 大人と子供 | 熟字训 | 大人=オトナ | 大人→おとな（整词） |
| 眼鏡をかけた博士 | 熟字训 | 眼鏡=メガネ / 博士=ハカセ | 眼鏡→めがね、博士→はかせ（整词） |
| 10分で終わる | 助数词 | 分=ブン（错） | 10→じゅっ、分→ぷん |
| 3本の鉛筆 | 助数词连浊 | 本=ポン（错） | 3→さん、本→ぼん |
| 9時に3階へ | 助数词连浊 | 階=カイ | 階→がい |
| 二十歳になった | 助数词融合 | 二十=ニジュウ 歳=サイ（错） | 二十歳→はたち（整体） |
| 4月1日 | 日期 | 月=ガツ 日=カ（错） | 4→し、月→がつ、1日→ついたち |
| 六千人が集まる | 数字混合 | 六千=ロクセン（整词） | 六→ろく、千→せん、人→にん |
| 1万6千人が来る | 数字混合 | 万=マン 千=セン（数字丢失） | 万→まん、千→せん、人→にん |
| 合計 | 字素对齐 | 合計=ゴウケイ（整词） | 合→ごう、計→けい |
| 図書館 | 字素对齐 | 図書=トショ + 館=カン | 図→と、書→しょ、館→かん |
| 競争 | 音读表 DP | 競争=キョウソウ（整词） | 競→きょう、争→そう |
| 気持ちがいい | 训读词干 | 気持ち=キモチ（整词） | 気→き、持→も |
| 一石二鳥だ | 四字熟语 | 一=イチ 石=コク 二鳥=ニチョウ（错） | 一石二鳥→いっせきにちょう |
| 王菲のファン | 未知词 | 菲 无读音 | 菲→unk，不注音 |

### 12.2 正误对照（体现引擎价值）

| 词 | ❌ 错误做法（按字查字典） | ✅ 引擎结果 |
|---|---|---|
| 今日 | こん + にち | きょう |
| 大人 | だい + じん | おとな |
| 風邪 | ふう + じゃ | かぜ |
| 20歳 | にじゅう + さい | はたち |
| 3本 | さん + ほん | さん + ぼん（连浊） |
| 上手 | うわ + て / かみ + て | じょう + ず |
| 競争 | せり + そう（单字误读） | きょう + そう |
| 図書 | ず + しょ（单字误读） | と + しょ |
| 一石二鳥 | いち + こく + にちょう | いっせきにちょう |

---

## 13. 局限与改进方向

1. **纯训读/熟字训拆字**仍是难点。例如「今年(ことし)」「下手(へた)」「生涯(しょうがい)」这类整体训读，拆到单字本身没有权威答案（「今」不单独读「こ」）。当前策略是能拆就拆、拆不动就退回整词读音，避免硬拆出错。

2. **人名/地名/专有名词**（光村、与謝野、長谷川）读音特殊，词典常不覆盖，依赖「未知词零猜测」原则宁可留空。

3. **连浊的展示粒度**：字读音优先用「基本形」（如「階」在词内读「がい」，但音读表存「かい」），更利于学习，但严格说与词内实际发音有细微出入。

4. **音读表为手工维护**：目前覆盖约 300 个高频字，遇到表外且 N-best 拆不出的字会退回整词读音。可持续扩充，或用更完整的「漢字音読データ」替换。

5. **双引擎依赖可选**：Sudachi 未装时只有单引擎，交叉验证这一层不生效，但主流程（Viterbi + 规则 + N-best + 字素对齐）已经能覆盖绝大多数情况。

---

*文档对应实现：`furigana.py`（注音引擎核心）、`textbook.py`（字读音接入与学习计划）。*
*所有示例均来自引擎真实运行输出。*
