# 日语汉字注音引擎技术文档

> 深入解析本项目 `furigana.py` 如何实现「汉字 → 平假名读音」的高准确率自动标注。
> 面向对算法实现不太熟悉、但想了解原理的读者（例如需要向日语教师解释技术方案时）。

---

## 目录

1. [为什么汉字注音这么难](#1-为什么汉字注音这么难)
2. [整体架构：五层防错 + 字读音提取](#2-整体架构五层防错--字读音提取)
3. [第 1 层：形态素解析与 Viterbi 解码](#3-第-1-层形态素解析与-viterbi-解码)
4. [第 2 层：数词与助数词规则引擎](#4-第-2-层数词与助数词规则引擎)
5. [第 3 层：N-best 歧义检测](#5-第-3-层n-best-歧义检测)
6. [第 4 层：双引擎交叉验证](#6-第-4-层双引擎交叉验证)
7. [第 5 层：未知词零猜测](#7-第-5-层未知词零猜测)
8. [送假名对齐：把整词读音拆到字](#8-送假名对齐把整词读音拆到字)
9. [核心：字读音提取（字素对齐）](#9-核心字读音提取字素对齐)
10. [关键辅助算法](#10-关键辅助算法)
11. [数据流与数据结构](#11-数据流与数据结构)
12. [实战例子汇总](#12-实战例子汇总)
13. [局限与改进方向](#13-局限与改进方向)

---

## 1. 为什么汉字注音这么难

给日语汉字自动标注读音（振假名/ルビ），本质是把一个「表意文字序列」映射成「假名序列」。难点在于**同一个汉字，读音随上下文变化**，大致有六类：

| 难点类型 | 例子 | 说明 |
|---|---|---|
| **多音字（音读 vs 训读）** | 「生」：生(い)きる / 学生(せい) / 一生(しょう) | 同一个字三种读音 |
| **同形异音词** | 「行った」：行(い)った / 行(おこな)った | 看上下文才能确定 |
| **熟字训** | 「今日」→ きょう、「風邪」→ かぜ | 整词读音，拆开就错 |
| **连浊（音便）** | 「3 階」→ さん**が**い、「火曜日」→ かよう**び** | 清音在复合词中浊化 |
| **数词 + 助数词** | 1 分→いっ**ぷ**ん、3 本→さん**ぼ**ん、20 歳→**はたち** | 数字+量词的读法极不规则 |
| **送假名** | 「食べる」→ 食(**た**)べる、「美味しい」→ 美味(**おい**)しい | 汉字只占读音的一部分 |

如果只靠「查字典」给每个汉字一个固定读音，以上六类几乎全错。所以本引擎采用**「词典引擎 + 规则引擎 + 交叉验证 + 字素对齐」的分层防错架构**。

---

## 2. 整体架构：五层防错 + 字读音提取

```
                    输入：一段日文文本
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第1层  MeCab(fugashi) + UniDic 词典            │
   │        · 把句子切成「词」(形态素)               │
   │        · Viterbi 算法在词格上找最优切分+读音     │
   │        · 每个词自带词典读音(kana)              │
   └───────────────────────┬───────────────────────┘
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第2层  数词 + 助数词 规则引擎                   │
   │        · 修正 UniDic 对「10分/3本/1日」的错误   │
   └───────────────────────┬───────────────────────┘
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第3层  N-best 歧义检测                          │
   │        · 同形异音词(行った/辛い) → 标警示+候选   │
   └───────────────────────┬───────────────────────┘
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第4层  Sudachi 第二引擎交叉验证                  │
   │        · 双引擎一致=高置信；分歧=降置信并给读音   │
   └───────────────────────┬───────────────────────┘
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 第5层  未知词零猜测原则                          │
   │        · 词典无据 → 明确标「读音未知」不瞎猜     │
   └───────────────────────┬───────────────────────┘
                           │
                    输出：token 列表（词 + 整词读音）
                           │
   ┌───────────────────────▼───────────────────────┐
   │ 扩展层  字读音提取（字素对齐 Grapheme-to-Phoneme）│
   │        · 把「整词读音」拆到「每个汉字」           │
   │        · 用于课本「单字」的假名标注               │
   │        · 合計→合(ごう)+計(けい) 这类拆解          │
   └───────────────────────┬───────────────────────┘
                           │
                    输出：每个汉字各自的读音
```

前五层回答「**这个词怎么读**」，扩展层回答「**这个词里每个字分别怎么读**」。两个问题都要解决，才能真正做好注音。

---

## 3. 第 1 层：形态素解析与 Viterbi 解码

### 3.1 什么是「词格（Lattice）」与 Viterbi

日语句子没有空格，机器要先「分词」。以「東京都に住む」为例，可能的分词方式有：

```
東京都 / に / 住む        ← 正确
東京 / 都 / に / 住む     ← 也可能是对的
東 / 京都 / に / 住む     ← 错误（读成 ひがし・きょうと）
```

MeCab 的做法是把每个字的位置都当作「可能切分点」，把所有可能的分词画成一张**图（词格）**：

```
位置:  0      1      2      3      ...
       東 ── 東京 ── 東京都
        │      └─── 都
        └── 京 ── 京都
             └── 都
```

图中每条「边」是一个候选词，带有一个「代价（cost）」，由词典中的词频、前后连接概率决定。**Viterbi 算法**就是动态规划：从起点走到终点，找一条「总代价最小」的路径。

> 通俗版：Viterbi = 「把每个岔路口的所有选择都记下来，最后回退找出全局最优的一条路」，而不是贪心地每一步选当前最好。

关键点：**UniDic 词典里每个词条同时带有它的读音**。所以分词正确了，整词读音也就正确了——这从根源上解决了「多音字」「同形异音词」的大部分问题（因为读音是跟着「词」走的，而不是跟着「字」走的）。

```
MeCab 输出示例：
  東京都  →  とうきょうと   (整词读音)
  に      →  (无汉字，无需注音)
  住む    →  すむ
```

### 3.2 为什么上下文决定读音

同一个「行」，在不同词里有不同读音，而「词」是由上下文切分出来的：

```
学校に行った。   →   行(い)った        ← 动词「行く」的连用形
会議を行った。   →   行(おこな)った    ← 动词「行う」的连用形
```

因为 UniDic 把「行った」在不同语境下归到不同词条（行く / 行う），所以读音自动正确。**这就是「上下文决定读音」的底层机制。**

---

## 4. 第 2 层：数词与助数词规则引擎

UniDic 对普通词很准，但对「数字 + 量词」经常出错（它可能把「10分」读成「じゅう・ふん」而不是「じゅっぷん」）。所以加一层**规则引擎**专门处理数词。

### 4.1 数字 → 假名的读法生成

内置了一套「数字读法表」，可以精确生成任意整数（支持到千亿级）的普通读法：

```python
_ONES      = ['', 'いち', 'に', 'さん', 'よん', 'ご', 'ろく', 'なな', 'はち', 'きゅう']
_TENS      = ['', 'じゅう', 'にじゅう', ...]
_HUNDREDS  = ['', 'ひゃく', 'にひゃく', 'さんびゃく', ...]   # 注意 3 百=さん「びゃく」
_THOUSANDS = ['', 'せん', 'にせん', 'さんぜん', ...]          # 注意 3 千=さん「ぜん」
```

`num_to_kana(6000)` → `ろくせん`；`num_to_kana(2017)` → `にせんじゅうなな`。

### 4.2 助数词（量词）特殊读音表

内置了约 40 个常用量词的**不规则读法表**，每个量词存「基本读音」+「特殊尾数的完整读法」：

```python
'分': {'base':'ふん', 1:'いっぷん', 3:'さんぷん', 6:'ろっぷん', 8:'はっぷん', 10:'じゅっぷん'}
'本': {'base':'ほん', 1:'いっぽん', 3:'さんぼん', 6:'ろっぽん', ...}
'歳': {'base':'さい', 20:'はたち'}           # 20 岁读「はたち」，完全不规则
'日': {'base':'にち', 1:'ついたち', 2:'ふつか', 3:'みっか', ...}   # 日期读法
```

### 4.3 拆解与融合

引擎把「数字+量词」的读音**拆成「数字部分」和「量词部分」**，便于前端在汉字上方精确标注：

```
「3本」 → 数字部分「さん」 + 量词部分「ぼん」
「20歳」 → 无法拆分 → 整体「はたち」（标记为 FUSED）
「3日間」 → 「みっ」 + 「かかん」
```

这样每个字都能对上自己的那一段读音，而不是整段糊在一起。

---

## 5. 第 3 层：N-best 歧义检测

有些词即使分对了，仍可能有两个读音（真歧义）。此时用 MeCab 的 **N-best** 功能：不只返回 1 条最优路径，而是返回前 N 条，看同一个表面片段在不同路径下读音是否分歧。

```
「辛い」 → 路径1: からい(辣)  路径2: つらい(痛苦)  → 真歧义，标警示+给候选
「火曜日」→ かようび / かようひ → 只是清浊差，不算歧义（见第10节清浊归一）
```

真歧义会被标记为 `c='low'` 并给出 `alt`（候选读音列表），让用户知道「这个字可能有别的读法」。

---

## 6. 第 4 层：双引擎交叉验证

引入第二个形态素分析引擎 **Sudachi**（模式 C，倾向最长复合词）。两个引擎独立分析，互相印证：

| 情况 | 处理 |
|---|---|
| 两引擎读音一致 | 置信度升为 `high` |
| 两引擎分歧（非清浊差） | 采用主读音，把对方读音列入候选，降置信 |

典型价值：MeCab 的短单位切分可能把「日本人」拼成「にっぽん + じん」（错误），Sudachi 长词模式直接给出「にほんじん」，交叉验证就能纠偏。

> 注：Sudachi 是可选依赖，装了才启用；没装时自动跳过该层，不影响主流程。

---

## 7. 第 5 层：未知词零猜测

如果两个引擎都查不到这个词（人名、外来新词等），**绝不乱猜**，而是明确标记 `unk=True`、读音为空。宁可显示「未知」，也不给一个看起来对其实是错的读音——这对学习工具至关重要。

```
王菲のファンです。  →  「菲」词典未收录 → unk=True，不注音
```

---

## 8. 送假名对齐：把整词读音拆到字

词典给出的是「整词读音」，但前端要在「单个汉字」上方标注假名，需要知道整词读音里**哪一段属于汉字、哪一段属于假名**。

以「食べ物」为例，整词读音「たべもの」，表面形由「食（汉字）+ べもの（假名）」组成。做法：

1. 把表面形按「汉字 / 非汉字」分段：`食`（汉字段）+ `べもの`（假名段）
2. 用正则把读音「たべもの」匹配成：汉字段读音 `た` + 假名段 `べもの`
3. 于是「食」→ `た`，完美对齐

```
食べ物    → 食(た) + べもの(无注音)
美味しい  → 美味(おい) + しい(无注音)
```

这一步很关键：它让「汉字部分」和「假名部分」的读音边界精确对齐，是后面「字读音提取」的第一优先级来源。

---

## 9. 核心：字读音提取（字素对齐）

> 这是本引擎最核心、也最体现「准确识别」的部分。

### 9.1 问题定义

词典能给出「合計」整词读「ごうけい」，但学习工具需要在「合」上方标「ごう」、「計」上方标「けい」。**把整词读音切分到每个汉字**，就是「字素对齐（Grapheme-to-Phoneme）」。

难点：日语音读的切分边界无法从读音本身看出来（「ごうけい」可以切成「ごう+けい」或「ご+うけい」）。所以必须借助**外部知识**。

### 9.2 四层对齐策略（按优先级从高到低）

#### 策略 A：单字 / 送假名词干 —— 直接用对齐结果

如果 `annotate` 已经把词拆成了「单字」或「汉字词干 + 送假名」，则 token 自带的 `r` 就是该字的读音：

```
多い   → 多(おお)         ← 送假名「い」已剥离
代わり → 代(か)            ← 送假名「わり」已剥离
館     → 館(かん)          ← 单独成词
```

#### 策略 B：N-best 单字切分 + 拼接校验

对于纯多汉字词（合計、勉強），用 MeCab 的 N-best 强制找「逐字切分」的路径，并**校验所有单字读音拼接后是否等于整词读音**。只有相等才采纳——这一步是防止把整词读音糊到单字上的关键保险。

```
「合計」的 N-best 路径里包含：
   [合(ゴウ), 計(ケイ)]  → 拼接 = ごうけい = 整词读音 ✓ → 合=ごう, 計=けい
「勉強」：
   [勉(ベン), 強(キョウ)] → べんきょう ✓ → 勉=べん, 強=きょう
```

#### 策略 C：音读表 + 动态规划回溯

N-best 有时拆不出来（例如「競争」的单字候选里「競」只有「せり/けい」，没有「きょう」）。此时启用**内置常用汉字音读表** `_ON_YOMI`，用**动态规划（回溯）**做字素对齐：

```
音读表：
  競 → [きょう, けい]
  争 → [そう]

输入：競争 / きょうそう
DP 过程：
  位置0 选「競」的候选「きょう」(长度3) → 剩余「そう」
  位置1 选「争」的候选「そう」  (长度3) → 剩余空 ✓
  结果：競=きょう, 争=そう
```

DP 的核心：`dfs(i, pos)` 表示「已切分前 i 个字，读音消耗到 pos 位置」，逐个尝试每个字的候选读音，能做到底（恰好切完整个读音）就成功。若多条路径都可行，取第一条（候选已按常用度排序）。

#### 策略 D：训读前缀匹配 + 数字混合

- **训读词干**：如「気持ち」的词干「気持」读「きも」，其中「持」的训读是「もち」，词干只剩「も」。DP 允许候选读音是原文片段的前缀扩展（「もち」→ 前缀「も」），从而拆出 気=き、持=も。
- **数字混合**：如「6千」读「ろくせん」，数字部分用规则引擎读出「ろく」，单位「千」用固定读「せん」，剥离后「千」= せん。

### 9.3 为什么这套能「准确」

```
核心思想：用「整词读音」作为校验锚点。
```

任何对齐结果，都必须满足「**所有单字读音拼起来 = 整词读音**（清浊归一后）」这一硬约束。这样即便某个字的候选读音有多个，也能被整词读音「逼」出正确的那一个。加上候选来源多样（词典 nbest、音读表、规则引擎、训读），层层兜底，最终准确率很高。

---

## 10. 关键辅助算法

### 10.1 清浊归一（连浊忽略）

日语复合词中清音常浊化（连浊），但「か vs が」在判断读音是否正确时应该视为「同一个音」：

```python
_DEVOICE = {'が':'か', 'ざ':'さ', 'だ':'た', 'ば':'は', 'ぱ':'は', ...}
_devoice('さんがい')  # → さんかい
```

用途：`is_rendaku_variant('かようび', 'かようひ')` → True（只是清浊差，不警示）。

### 10.2 语体等价变体

口语与书面语的等价读法，不算歧义：

```
わたくし = わたし、いう = ゆう、にほん = にっぽん、よい = いい ...
```

### 10.3 片假名 → 平假名

```
カ → か（Unicode 码位减 0x60）
```

### 10.4 缓存（LRU）

字素对齐 `_split_kanji_reading` 和单字候选 `_char_candidates` 都加了 `functools.lru_cache`。同一批课本里「合計」可能出现几十次，第一次算好后直接命中缓存，避免重复做昂贵的 N-best 查询。

---

## 11. 数据流与数据结构

### 11.1 token 结构（第 1-5 层输出）

```python
{
    's':   '合計',          # 表面形（原文片段）
    'r':   'ごうけい',      # 该片段的平假名注音（无汉字则为 None）
    'w':   '合計',          # 所属整词
    'wr':  'ごうけい',      # 整词读音
    'dw':  '合計',          # 词典原形（用于活用词，如「貧しかっ→貧しい」）
    'dr':  'ごうけい',      # 原形读音
    'c':   'high',          # 置信度 high/mid/low
    'alt': ['...'],         # 候选读音（仅歧义时）
    'unk': False,           # 是否词典未收录
}
```

### 11.2 字读音提取（扩展层输出）

```python
extract_char_readings(tokens) → [('合', 'ごう'), ('計', 'けい'), ...]
```

课本「字」索引把每个汉字对应到这个字读音，存入 `book_kanji.reading`，前端用 `<ruby>計<rt>けい</rt></ruby>` 渲染。

### 11.3 完整数据流

```
日文句子
  → annotate()           第1-5层，输出 token（整词读音）
      ├─ extract_kanji_words()   → 汉字→所属词→词读音（全库 kanji_index 用）
      └─ extract_char_readings() → 汉字→字读音（课本 book_kanji 用，字素对齐）
  → 前端 <ruby> 渲染
  → 学习计划 / 艾宾浩斯 SRS（字、词双轨）
```

---

## 12. 实战例子汇总

| 输入 | 难点 | 引擎处理结果 |
|---|---|---|
| 学校に行った | 多音字「行」 | 行→い（动词「行く」） |
| 会議を行った | 多音字「行」 | 行→おこな（动词「行う」） |
| 今日は良い天気 | 熟字训 | 今日→きょう、良→よ |
| 風邪を引いた | 熟字训 | 風邪→かぜ（不拆成 かぜ+じゃ） |
| 10分で終わる | 助数词 | 10→じゅっ、分→ぷん |
| 3本の鉛筆 | 助数词连浊 | 3→さん、本→ぼん |
| 20歳になった | 助数词融合 | 20歳→はたち（整体） |
| 4月1日 | 日期 | 4月→しがつ、1日→ついたち |
| 六千人が集まる | 数字混合 | 六→ろく、千→せん、人→にん |
| 1万6千人 | 数字混合 | 万→まん、千→せん、人→にん |
| 合計 | 字素对齐 | 合→ごう、計→けい |
| 図書館 | 字素对齐 | 図→と、書→しょ、館→かん |
| 競争 | 音读表 DP | 競→きょう、争→そう |
| 上手 | 熟字训 | 上→じょう、手→ず |
| 気持ちがいい | 训读词干 | 気→き、持→も |
| 一石二鳥 | 四字熟语 | 一石二鳥→いっせきにちょう（整词） |
| 王菲のファン | 未知词 | 菲→unk，不注音 |

---

## 13. 局限与改进方向

1. **纯训读/熟字训拆字**仍是难点。例如「今年(ことし)」「下手(へた)」这类整体训读，拆到单字本身没有权威答案（「今」不单独读「こ」）。当前策略是能拆就拆、拆不动就退回整词读音，避免硬拆出错。

2. **人名/地名/专有名词**（光村、与謝野、長谷川）读音特殊，词典常不覆盖，依赖「未知词零猜测」原则宁可留空。

3. **连浊的展示粒度**：字读音优先用「基本形」（如「階」标「かい」而非词内连浊「がい」），更利于学习，但严格说与词内实际发音有细微出入。

4. **音读表为手工维护**：目前覆盖约 300 个高频字，遇到表外且 N-best 拆不出的字会退回整词读音。可持续扩充，或用更完整的「漢字音読データ」替换。

---

*文档对应实现：`furigana.py`（注音引擎核心）、`textbook.py`（字读音接入与学习计划）。*
