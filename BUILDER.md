# 🧩 组句练习（多邻国式拼句）—— 设计与实现完整解析

> 模块：`sentence_builder.py` ｜ 测试：`test_builder.py` ｜ 前端：`static/index.html`（我的课本 → 🧩 组句练习 / 🧩 组句配置）

## 1. 功能总览

根据现有语料（`sentences` 全库语料 / `book_sentences` 课本句子）自动生成
Duolingo 风格的题目：

| 题型 | 说明 |
|------|------|
| **arrange 组句题**（核心） | 句子按「文节」拆成词块并打乱，用户点击词块按顺序拼回句子 |
| **pairs 配对题** | 从语料注音里抽词，词形 ↔ 读音连线（读音唯一才入选，保证无歧义） |

题型占比可配置（默认 组句 80% / 配对 20%，自动归一）。

## 2. 两条完全独立的难度轴

这是本模块的核心设计约束：**「初级/高级模式」与「N 级难度」互不耦合，自由组合**。

### 2.1 mode —— 题目形态轴（basic / advanced）

* **basic 初级**：词块 = 恰好所需，无任何干扰项。默认开启**语料同位换词**：
  题目不一定是原句 —— 引擎会把原句中某个「名词+格助词」成分换成语料中
  与**同一助词、同一谓语**真实共现过的另一个名词（如 `本を読む → 小説を読む`、
  `図書館で読む → 電車で読む`、`雨が降る → 雪が降る`），换词后的整句必须
  通过 `grammar.check_sentence_grammar` 语法质检才会成为题目。
  搭配统计来自语料共现挖掘（`_colloc_nouns`），サ变结构用「名词+する」做
  搭配键，防止所有「〜をする」互相污染；`する/いる/ある/なる` 等泛化动词
  不作为换词依据。
* **advanced 高级**：在所需词块之外混入**干扰词块**（数量/种类可配）：
  - `verb_form` 时态/礼貌体/极性变形：`読みました → 読みます / 読みません`、
    タ形 → 辞书形（用 UniDic `orthBase` 重建，如 `行った → 行く`）；
  - `particle` 换助词的同名词块：`これを → これまで`；
  - `vocab` 语料随机高频句外词。

### 2.2 level —— 句子选材轴（any / N5..N1）

每个候选句先经 `grammar.analyze` 解析，**句内最高级别语法点**即句子难度。
`level=N3` 时优先选 N3 句；题量不足自动放宽（题面上带 `level_relaxed` 标记）。
与 mode 无任何耦合：`初级×N1`、`高级×N5` 均为合法组合。

## 3. 多解稳健判卷（关键设计）

日语是格标记语言：格成分（〜は/〜が/〜を/〜に/〜で、时间词、副词）在小句内
可自由换序（scrambling）。**判卷绝不能只认原句一种语序** —— 引擎的处理链：

### 3.1 文节切块（`parse_sentence`）

MeCab+UniDic 形态素 → 文节：内容词 + 其后附属功能词为一块。所有**固定依存
一律焊死在块内**，块间只剩自由格成分：

* 连体词并入被修饰块：`この薬を`；
* 属格 `〜の` 并入后块：`私の友達は`；含动词的 `〜の`（`買ったばかりの`）
  视为连体从句，**并吸收前置时间副词**（`昨日買ったばかりのカメラを` 一体，
  防止「昨日」漂移后改变修饰对象、造成语义性误判）；
* 连体形从句 + 名词合并：`帰ることにした`、`上手になるために`；
* 补助动词链附着：`読んでいる / なくしてしまった / 開けてもいい /
  中止になりました / かもしれない / 練習している`（サ变名词+する）；
* 程度副词并入形容词块：`とても高い`。

**重建不变式**：全部词块拼接必须与原句去标点后逐字一致，否则弃句。

### 3.2 分区（zone）

读点、接续助词（〜ば/〜て、/〜ので/〜から…）处切区：**区序固定、区内自由**，
从句成分不允许越过逗号漂移。每区最后一块（谓语）固定区尾；句首接续词/感叹词
固定区头；出现无格助词又非副词性的块时**整区锁死**（宁可少给自由度，绝不误判）。

### 3.3 合法语序 = 各区自由块全排列的组合

```
私は | 昨日 | 図書館で | 本を | 読みました   → 4! = 24 种语序全部判对
雨が | 降っているので ‖ 試合は | 中止になりました → 区内各 1 种，跨区必错
彼は | 来ないかもしれないと | 言っていた   → 引用と句可前置，2 种判对
```

### 3.4 双保险判卷

* 组合数 ≤ `perm_limit`（默认 120）时：出题即枚举全部合法排列，**逐句拼回
  整句过 `grammar.check_sentence_grammar` 质检**，通过者写入 `accepted`
  白名单；判卷 = 白名单精确匹配。
* 组合数超限时：退化为**约束判卷**（区完整性 + 区头/区尾固定 + 区内自由）。
* 两条路径的目标一致：**所有语法正确的语序都判对，一切结构性错误都判错**。

### 3.5 结构化反馈

判错时给出针对性提示：`用到了干扰词块` / `少用了词块：本を` /
`「読みました」是谓语部分，必须放在分句末尾` / `分句成分越过了逗号/接续边界` /
`语序固定`，并返回规范答案与合法语序总数（前端展示
「此题共有 N 种语法正确的语序，你的答案是其中之一」）。

### 3.6 干扰块公平性

判卷要求「所需词块恰好全用」，用任一干扰块必错。为保证判错公平：

* **意义保持型助词替换一律禁止生成**（`は↔が`、`に↔へ`、`は↔も`、`が↔も`）——
  这类替换后句意几乎不变，判错会不公平；
* 其余干扰块（时态变形/句外词/非等价换助词）语义必然偏离题面译文，
  判错有充分依据；
* 干扰块 surface 不得与任何所需块相同、不得是句内子串。

## 3A. 语义正确性架构：算法不懂句义，为什么判卷仍然绝对准确

核心思路：**把语义判断从运行时算法中彻底剥离**，交给两个外部
「语义预言机」，打分机只做纯集合/序列运算，零语义、零猜测：

| 语义预言机 | 锚定什么 | 用在哪 |
|------------|----------|--------|
| **人工译文**（`sentences.translation`） | 本题唯一正确语义 | 干扰块判错的公平性依据：时态变形/句外词/换助词的语义必然偏离译文 |
| **语料分布实证**（共现挖掘 `_colloc_nouns`） | 哪些替换合法 | 换词与多答案的接受依据：「小説を読む」在语料中真实出现过 = 有人写过 = 搭配成立（分布语义学） |

### 3A.1 公平性铁律：无译文 ⇒ 零干扰块

干扰块判错的全部依据是「语义偏离译文」。若句子**没有译文**，题面只能要求
「语法正确即可」——此时任何语法上可行的块都不该被判错，因此
`_build_arrange` 对无译文句**一律不生成干扰块**（多答案与语序多解照常）。
这是防止冤判的结构性保证，不依赖任何语义理解。

### 3A.2 混淆项 vs 多答案：不是二选一，而是按语义锚互补

* **混淆项（guaranteed-wrong by construction）**：只在有译文时投放，
  且每一类都能**机械证明**其错误性——
  - `verb_form`：时态/极性/礼貌体变形 ⇒ 与译文时态必然不符；
  - `particle`：等价对已禁 ⇒ 换助词必然改变格关系；
  - `vocab`：句外词 + **语料实证反查**——凡与本句任一槽位（同助词+同谓语）
    在语料中共现过的词一律不投放，因为它其实能合法替入槽位
    （那是多答案的领域），投放会造成不公平判错。
* **多答案（interchangeable by attestation）**：高级模式为一个
  「名词+格助词」槽位追加 1–4 个可互换名词块（`本を／小説を／新聞を`、
  `ガットが切れた／期限が切れた／ガソリンが切れた`），**任选其一均判对**。
  五重门禁（全部机械可判，压力测试逐条驱动出来的真实防线）：
  1. **语料共现实证**：候选必须与同一助词+同一谓语在语料中真实共现，
     完整复合名词左扩收取（`注意事項` 不会被截成 `事項`）；
  2. **候选词终审** `_noun_candidate_ok`：纯实义名词、无形式名词头
     （`こと煙草` 这类挖掘碎片拒绝）、无数字/助数词头；
  3. **格限定**：槽位只认选择限制强的 `を`(宾语)/`が`(主语)/`で`(场所·手段)；
     `に` 格语义角色过宽（方向/目标/时间/惯用：`文頭に持ってくる` ≠
     `手もとに持っている`），一律不做槽位；
  4. **斜格闸门**：句中存在「名词+に/へ/と/から/まで」成分时整句禁用换词
     （`塀に…掛けた` 中的 `塀に` 把 `掛ける` 收窄为「搭靠」义，
     `電話を` 虽与 `掛ける` 共现也不能替入）—— 宁可少出，绝不出错；
  5. **换入整句过语法引擎 + 结构等价**（块数不变，语序判卷不受影响）。

### 3A.3 打分机 = 三步纯机械运算

```
① tile_map:  词块 → 槽位编号（可互换块映射同一槽位；干扰块 → -1）
② 多重集:    所用词块的槽位集合必须恰好 = {0..n-1}
             （用干扰块→含 -1 必错；同槽双用→重复必错；与选哪个词无关）
③ 语序:      槽位序列 ∈ accepted 白名单（或区约束兜底）
```

运行时不做任何语义/语法计算——语法在出题时枚举质检完毕，语义由
两个预言机在数据层钉死。打分机因此**简单、可解释、可穷举测试**。

### 3A.4 多样性保证

* 换词/多答案候选从共现 top-20 中**随机抽样**（非固定取最高频）；
* 干扰块三类来源独立随机、每次组合不同；
* 压力测试 F 节验证：同一句 12 次出题，换词/多答案/干扰组合均不唯一。

## 4. 质检链（每道题出题前）

1. `grammar.check_sentence_grammar`：完整单句、有谓语、句尾合规、无伪假名串；
2. 无引号/括号（结构险区）；长度 6–60 字；
3. 文节数在 `[min_tiles, max_tiles]`（默认 3–9）；
4. 词块重建逐字校验；
5. （`strict_check`，默认开）全部合法排列语法质检。

## 5. 配置（`/api/builder/cfg`，settings 键 `sentence_builder_cfg`）

| 键 | 默认 | 说明 |
|----|------|------|
| `enabled` | true | 总开关 |
| `mode` | basic | 初级/高级（题目形态轴） |
| `level` | any | N 级难度（选材轴，与 mode 独立） |
| `scope` | corpus | corpus / book / mixed / lyric（v20：KTV 歌词行为题源；无译文 ⇒ 公平性铁律自动生效，advanced 也零干扰，题面带 🎵 歌名） |
| `count` | 6 | 每组题数（1–20） |
| `types` | 80/20 | 组句/配对占比（自动归一） |
| `min_tiles` / `max_tiles` | 3 / 9 | 词块数范围 |
| `distractors` | 3 | 高级干扰块数（0–6，无译文句强制 0） |
| `distractor_kinds` | 全开 | verb_form / particle / vocab |
| `alt_answers` | true | 高级多答案：槽位可互换名词块 |
| `alt_max` | 2 | 每题最多追加可互换名词数（1–4） |
| `alt_min_freq` | 1 | 可互换名词的语料共现次数下限 |
| `swap` / `swap_prob` | true / 0.35 | 初级语料换词及概率 |
| `strict_check` | true | 全排列语法质检 |
| `perm_limit` | 120 | 白名单枚举上限 |
| `show_furigana` | basic | 词块注音：仅初级/始终/从不 |

所有键白名单过滤 + 边界修正（脏值自动回落安全默认），字典键深合并。

## 6. API

| 路由 | 方法 | 说明 |
|------|------|------|
| `/api/builder/cfg` | GET/POST | 读取（含近 14 天统计）/ 保存配置 |
| `/api/builder/quiz` | POST | 生成一组题；`mode`/`level`/`scope`/`count`/`book_ids` 可临时覆盖 |
| `/api/builder/check` | POST | 稳健判卷 `{spec, order}` → `{ok, feedback, canonical, alt_count, your_text}` |
| `/api/builder/answer` | POST | 记录结果（按日 + 题型/模式/级别细分） |
| `/api/builder/stats` | GET | 最近 N 天统计 |

## 7. 测试

### 7.1 单元/集成（`python3 test_builder.py`）

九个板块，临时库隔离：切块正确性与重建不变式（300 句语料抽查）、
24 种自由语序全判对/谓语错位/跨区/缺块/重复/干扰块/脏输入判错、
白名单枚举与约束兜底、换词变体语法质检、等价助词禁用表、
**公平性铁律（无译文零干扰块）**、**多答案（互换块判对/同槽双用判错/
换入整句语法复验）**、配置白名单/边界/两轴独立、
basic/advanced/level 过滤出题主流程、API 全链路。

### 7.2 全语料深度压力（`python3 test_builder_stress.py`）

在**全部 15829 句真实语料**上验证六大机械不变式（算法不懂句义也必须成立）：

| 节 | 内容 | 规模 |
|----|------|------|
| A | 切块普查：逐字重建 / 分区恰好划分 / 语序数 ≥1 | 全库 15829 句（94% 可解析，失真 0） |
| B | 白名单全枚举：每个接受语序独立复验语法引擎 + 纯排列不变式 | 300 句 × 2495 个语序 |
| C | 出题不变式：basic 纯净 / advanced 干扰安全（等价对/子串/撞面/实证反查）/ 无译文零干扰 | 双模式 96 道题 |
| D | 多答案：互换块换入整句语法复验 + 判卷任选均对 + 同槽双用必错（JSON 往返模拟 API） | 逐块复验 |
| E | 对抗判卷：谓语前置/缺块/多块/随机乱序与白名单逐一比对一致 + 300 次垃圾输入模糊测试不崩溃 | 120 次乱序 + 300 次 fuzz |
| F | 多样性：同句 12 次出题，换词/多答案/干扰组合不唯一 | — |
